# ZTAMP Planner Agent Backend
# Python 3.10+

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# LLM Communication
httpx>=0.27.0

# Database (PostgreSQL + pgvector)
psycopg2-binary>=2.9.9

# Embeddings (must match vectorize.py model)
sentence-transformers>=3.0.0

# Data validation
pydantic>=2.10.0

# Document Processing (IBM Docling) — structure-aware parser
docling>=2.70.0
docling-core[chunking]>=2.0.0
